'use strict';

var assert = require('chai').assert;
var ndarrayTransfiniteInterpolation = require('../');

describe('ndarray-transfinite-interpolation', function () {
  it('passes a test', function () {
    assert(ndarrayTransfiniteInterpolation());
  });
});
